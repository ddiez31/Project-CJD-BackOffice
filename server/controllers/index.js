const users = require('./users');
const regions = require('./regions');
const sections = require('./sections');
const type_users = require('./type_users');
const contributions = require('./contributions');

module.exports = {
  users,
  regions,
  sections,
  type_users,
  contributions
};
